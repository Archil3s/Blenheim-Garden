# Cloudflare deploy guard verification

This branch exists only to run the pull-request build against the production tree containing the OpenNext duplicate-env guard and the restored lightweight `/3d` renderer.
