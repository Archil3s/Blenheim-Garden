# Pushed 3D upgrade work

The branch now contains the implementation modules for Issue #44: plant habit profiles, deterministic natural variation, uploaded-artwork sizing, low-poly fallback transforms, contact shadows, soil/timber/grass/path materials, soil/wood/path/ground detail, trellis netting, climbing vines, tree branches, camera presets, richer inspector data, selection styling and performance caps.

These changes are isolated on the feature branch so production `main` remains untouched until the renderer integration and required browser verification are complete.
