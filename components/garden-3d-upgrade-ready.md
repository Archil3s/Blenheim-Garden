# Renderer integration ready

The reusable visual-upgrade layer is now pushed. The next commit should wire these helpers into `garden-webgl-visual.tsx`, then run the repository-required lint/build/Chromium checks before merge.
