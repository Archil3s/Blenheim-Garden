name: Build

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: webgl-exact-live-layout
          fetch-depth: 0
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.2.15
      - name: Fix Live 3D TypeScript types
        run: node scripts/fix-webgl-live-types.mjs
      - name: Commit Live 3D type fix
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Fix Live 3D layout TypeScript types"
            git push origin HEAD:webgl-exact-live-layout
          fi
      - run: bun install
      - run: bun run build
