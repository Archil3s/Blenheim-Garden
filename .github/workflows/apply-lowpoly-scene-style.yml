name: Apply low-poly scene style

"on":
  push:
    branches:
      - main
    paths:
      - .github/workflows/apply-lowpoly-scene-style.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.2.15
      - name: Apply cartoon scene styling
        run: python scripts/apply-lowpoly-scene-style.py
      - name: Install dependencies
        run: bun install --frozen-lockfile
      - name: Validate production build
        run: bun run build
      - name: Commit validated low-poly scene style
        run: |
          rm .github/workflows/apply-lowpoly-scene-style.yml
          rm scripts/apply-lowpoly-scene-style.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add components/garden-3d-unified.tsx components/garden-crop-patch-3d.ts components/garden-structure-3d.ts .github/workflows/apply-lowpoly-scene-style.yml scripts/apply-lowpoly-scene-style.py
          git commit -m "Apply cartoon low-poly style across 3D garden"
          git push
